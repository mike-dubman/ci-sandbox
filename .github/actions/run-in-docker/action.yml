name: 'Container from Dockerfile'
description: 'Get started dockerfile'
inputs: 
  myargs:
    description: 'docker args'
    default: ''
  file:
    description: 'Dockerfile name'
    default: ''
runs:
  using: 'docker'
  image: '.ci/Singularity.centos7'
  args:
    - ${{ inputs.myargs }}
